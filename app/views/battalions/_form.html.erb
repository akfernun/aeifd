<%= form_for(@battalion) do |f| %>
  <% if @battalion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@battalion.errors.count, "error") %> prohibited this battalion from being saved:</h2>

      <ul>
      <% @battalion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
