<%= form_for(@checkoff) do |f| %>
  <% if @checkoff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@checkoff.errors.count, "error") %> prohibited this checkoff from being saved:</h2>

      <ul>
      <% @checkoff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :checkofftype %><br>
    <%= f.text_field :checkofftype %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
